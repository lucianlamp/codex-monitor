# P8 Result: Real CLI commands over selected transports

## Status

Completed.

## Accepted

- Added fake WebSocket app-server integration tests for `ccb threads` and `ccb send`.
- Added default Codex App Unix socket path helper.
- Added boxed `AppServerTransport` support and endpoint opener.
- Wired explicit `ws://`, `stdio://`, managed, and App target transport selection.
- Wired `ccb threads --cwd` through initialize plus `thread/list`.
- Wired `ccb send --thread --text` through initialize plus `turn/start` completion wait.
- Added `agmsg watch` Task 9 stub.

## TDD Evidence

RED:

```text
cargo test --test fake_app_server
test send_command_waits_for_completion ... ok
test threads_command_lists_fake_thread ... FAILED
assertion failed: stdout.contains("thread-1")
```

GREEN:

```bash
cargo test --test fake_app_server
```

Result:

```text
test result: ok. 2 passed; 0 failed
```

Regression:

```bash
cargo test --test cli_contract
```

Result:

```text
test result: ok. 2 passed; 0 failed
```

Format:

```bash
cargo fmt --check
```

Result: passed.

## Remaining Risks

- `agmsg watch` intentionally returns a Task 9 error until the delivery loop is implemented.
