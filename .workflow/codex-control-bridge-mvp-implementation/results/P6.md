# P6 Result: agmsg SQLite source adapter

## Status

Completed.

## Accepted

- Added `AgmsgSource` under `sources::agmsg`.
- Added direct SQLite polling for `team`, `to_agent`, and `id > last_seen_id`.
- Returned rows in ascending id order.
- Converted matching messages into `BridgeEvent` values.
- Added `AGMSG_STORAGE_PATH`-aware default DB path resolution.
- Kept adapter read-only; no agmsg watcher, claim, or script side effects.

## TDD Evidence

RED:

```text
error[E0432]: unresolved import `codex_control_bridge::sources::agmsg::AgmsgSource`
```

GREEN:

```bash
cargo test --test agmsg_adapter
```

Result:

```text
test result: ok. 2 passed; 0 failed
```

Regression:

```bash
cargo test --lib delivery::tests
```

Result:

```text
test result: ok. 2 passed; 0 failed
```

Format:

```bash
cargo fmt --check
```

Result: passed.

## Remaining Risks

- Long-running polling and state updates are deferred to P9.
