# P9 Result: agmsg watch delivery loop

## Status

Completed.

## Accepted

- Added `State::last_seen` and `State::mark_seen`.
- Added adapter-level delivery formatting coverage for agmsg events.
- Implemented `ccb agmsg watch` as a foreground polling loop.
- Opens the selected Codex app-server transport once, initializes, polls SQLite, sends each event via `turn/start`, and persists last seen ids only after successful turn completion.
- Preserves source boundaries: agmsg adapter reads SQLite only; delivery loop owns Codex calls and state persistence.

## Verification Evidence

Focused adapter:

```bash
cargo test --test agmsg_adapter
```

Result:

```text
test result: ok. 3 passed; 0 failed
```

Fake app-server:

```bash
cargo test --test fake_app_server
```

Result:

```text
test result: ok. 2 passed; 0 failed
```

Full suite:

```bash
cargo test
```

Result:

```text
test result: ok. 23 passed; 0 failed
test result: ok. 3 passed; 0 failed
test result: ok. 2 passed; 0 failed
test result: ok. 2 passed; 0 failed
```

Format:

```bash
cargo fmt --check
```

Result: passed.

## Remaining Risks

- The watch loop is intentionally foreground and exits on Ctrl-C; no daemon/service mode is added.
