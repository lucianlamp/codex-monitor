# P3 Result: Transport trait, memory transport, and app-server client

## Status

Completed.

## Accepted

- Added `AppServerTransport` with `send`, `recv`, and `close`.
- Added `MemoryTransport` for deterministic client tests.
- Added `AppServerClient` request id allocation and pending response handling.
- Added initialize handshake that sends `initialize`, waits for the response, then sends `initialized`.
- Added `thread/list`, `thread/read`, and `turn/start` client helpers.
- Refused server requests with an explicit human-action error.
- Waited for `turn/completed` and failed non-completed terminal statuses.

## TDD Evidence

RED:

```text
running 5 tests
test client::tests::initialize_sends_initialize_and_initialized ... FAILED
test client::tests::server_request_is_refused ... FAILED
test client::tests::thread_list_sends_cwd_filter ... FAILED
test client::tests::thread_read_sends_include_turns ... FAILED
test client::tests::turn_start_waits_for_terminal_completion ... FAILED
```

GREEN:

```bash
cargo test --lib client::tests
```

Result:

```text
test result: ok. 5 passed; 0 failed
```

Regression:

```bash
cargo test --lib protocol::tests
```

Result:

```text
test result: ok. 7 passed; 0 failed
```

Format:

```bash
cargo fmt --check
```

Result: passed.

## Remaining Risks

- P3 uses only `MemoryTransport`; real ws, stdio, and Unix transports are deferred to P7.
