# P5 Result: Source event model, delivery formatting, and JSON state

## Status

Completed.

## Accepted

- Added `BridgeEvent` source event model.
- Added `sources::agmsg` module boundary for the adapter.
- Added agmsg delivery text formatting with team, recipient, sender, body, and reply instruction.
- Added generic source formatting with title and body.
- Added JSON `StateStore` with missing-file default load, parent directory creation, pretty JSON write, and reload.

## TDD Evidence

RED:

```text
cargo test --lib delivery::tests
test result: FAILED. 0 passed; 2 failed

cargo test --lib state::tests
test result: FAILED. 1 passed; 1 failed
```

GREEN:

```bash
cargo test --lib delivery::tests
```

Result:

```text
test result: ok. 2 passed; 0 failed
```

```bash
cargo test --lib state::tests
```

Result:

```text
test result: ok. 2 passed; 0 failed
```

Format:

```bash
cargo fmt --check
```

Result: passed.

## Remaining Risks

- State lock-file and long-running delivery loop behavior are deferred to later packets.
