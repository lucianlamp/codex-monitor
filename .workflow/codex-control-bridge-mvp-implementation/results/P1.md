# P1 Result: Scaffold Cargo package and naming contract

## Status

Completed.

## Accepted

- Added Cargo package `codex-control-bridge`.
- Added library crate `codex_control_bridge`.
- Added primary binary `codex-control-bridge`.
- Added alias binary `ccb`.
- Added `CLIENT_INFO_NAME = codex-control-bridge`.
- Added `CLIENT_INFO_TITLE = Codex Control Bridge`.
- Added initial README and CLI parser shell.

## TDD Evidence

RED:

```text
error[E0425]: cannot find value `CLIENT_INFO_NAME` in crate `codex_control_bridge`
error[E0425]: cannot find value `CLIENT_INFO_TITLE` in crate `codex_control_bridge`
```

GREEN:

```bash
cargo test --test cli_contract
```

Result:

```text
test result: ok. 2 passed; 0 failed
```

Format:

```bash
cargo fmt --check
```

Result: passed.

## Remaining Risks

- CLI command handlers are intentionally no-op until later packets.
