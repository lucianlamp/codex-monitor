# P4 Result: Target resolver and CLI command shell

## Status

Completed.

## Accepted

- Added endpoint selection contract: explicit endpoint, app target, managed default.
- Added thread summary parsing for `threads` and `items` response arrays.
- Supported direct thread objects and nested `{ "thread": ... }` objects.
- Supported direct `cwd` and nested `session.cwd`.
- Added single-thread resolution that rejects no matches and ambiguous matches.
- Added temporary CLI command summaries for `threads`, `send`, and `agmsg watch`.

## TDD Evidence

RED:

```text
running 5 tests
test target::tests::endpoint_explicit_wins ... ok
test target::tests::parses_items_and_nested_session_cwd ... FAILED
test target::tests::parses_thread_list_shapes ... FAILED
test target::tests::rejects_missing_thread_array ... FAILED
test target::tests::single_thread_resolution_rejects_ambiguous_matches ... ok
```

GREEN:

```bash
cargo test --lib target::tests
```

Result:

```text
test result: ok. 5 passed; 0 failed
```

Regression:

```bash
cargo test --test cli_contract
```

Result:

```text
test result: ok. 2 passed; 0 failed
```

Format:

```bash
cargo fmt --check
```

Result: passed.

## Remaining Risks

- CLI command handlers still print summaries; real transport-backed dispatch is deferred to P8.
