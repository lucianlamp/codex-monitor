# Packet P8

Objective: Wire real CLI commands to selected transports and fake app-server tests.
Ownership: `src/cli.rs`, `src/target.rs`, `src/transport/mod.rs`, `src/delivery.rs`, `tests/fake_app_server.rs`.
Verification: `cargo test --test fake_app_server`, `cargo test --test cli_contract`, `cargo fmt --check`.
