# P1 Discovery Result

Purpose: `codex-monitor` is a local-first Rust CLI/library bridge for
delivering external events into the Codex App / app-server control plane.

Important contracts:

- Cargo package: `codex-monitor`
- Library crate: `codex_monitor`
- Binaries: `codex-monitor` and `cdxm`
- Commands: `threads`, `send`, `agmsg watch`
- Default target: managed loopback WebSocket app-server
- App UI attach: explicit `--target app`
- agmsg is the first adapter, not the core product boundary

Repository state:

- Branch: `impl/mvp`
- `README.md` was dirty before this analysis and adds install/daily-flow docs.
- Existing tracked implementation workflow reports MVP completion.

