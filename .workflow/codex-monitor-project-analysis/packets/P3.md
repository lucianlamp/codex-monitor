# P3 Test Contract Audit

Objective: Identify what behavior is covered by tests and what remains
unproven.

Sources:

- `tests/cli_contract.rs`
- `tests/fake_app_server.rs`
- `tests/agmsg_adapter.rs`
- in-module tests under `src/`

Do not: run destructive or mutating live commands.

Expected output: covered contracts, skipped live surfaces, and risk notes.

Verification: run local test commands in P4.

