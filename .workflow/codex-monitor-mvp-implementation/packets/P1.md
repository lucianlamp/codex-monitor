# Packet P1

Objective: Scaffold Cargo package and naming contract.
Ownership: `.gitignore`, `Cargo.toml`, `README.md`, `src/lib.rs`, `src/main.rs`, `src/bin/cdxm.rs`, `src/cli.rs`, `tests/cli_contract.rs`.
Verification: `cargo test --test cli_contract`, `cargo fmt --check`.
