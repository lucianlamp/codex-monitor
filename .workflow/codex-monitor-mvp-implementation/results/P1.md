# P1 Result: Scaffold Cargo package and naming contract

## Status

Completed.

## Accepted

- Added Cargo package `codex-monitor`.
- Added library crate `codex_monitor`.
- Added primary binary `codex-monitor`.
- Added alias binary `cdxm`.
- Added `CLIENT_INFO_NAME = codex-monitor`.
- Added `CLIENT_INFO_TITLE = Codex Monitor`.
- Added initial README and CLI parser shell.

## TDD Evidence

RED:

```text
error[E0425]: cannot find value `CLIENT_INFO_NAME` in crate `codex_monitor`
error[E0425]: cannot find value `CLIENT_INFO_TITLE` in crate `codex_monitor`
```

GREEN:

```bash
cargo test --test cli_contract
```

Result:

```text
test result: ok. 2 passed; 0 failed
```

Format:

```bash
cargo fmt --check
```

Result: passed.

## Remaining Risks

- CLI command handlers are intentionally no-op until later packets.
