# P2 Result: Protocol builders and classifiers

## Status

Completed.

## Accepted

- Added app-server protocol builders for `initialize`, `initialized`, `thread/list`, `thread/read`, `turn/start`, and `thread/inject_items`.
- Added fixed `clientInfo` and `experimentalApi` handshake capability.
- Added incoming message classifier for responses, notifications, server requests, and unknown messages.
- Exported the protocol module from the library crate.

## TDD Evidence

RED:

```text
running 7 tests
test protocol::tests::classify_response_notification_and_server_request ... FAILED
test protocol::tests::initialize_has_fixed_client_info ... FAILED
test protocol::tests::initialized_is_notification ... FAILED
test protocol::tests::inject_items_is_explicit_raw_append ... FAILED
test protocol::tests::thread_list_uses_cwd_filter_and_limit ... FAILED
test protocol::tests::thread_read_can_include_turns ... FAILED
test protocol::tests::turn_start_wraps_text_input ... FAILED
```

GREEN:

```bash
cargo test --lib protocol::tests
```

Result:

```text
test result: ok. 7 passed; 0 failed
```

Regression:

```bash
cargo test --test cli_contract
```

Result:

```text
test result: ok. 2 passed; 0 failed
```

Format:

```bash
cargo fmt --check
```

Result: passed.

## Remaining Risks

- Protocol builders are only request-shape helpers until the transport client is added in P3.
