# P7 Result: WebSocket, stdio, and Unix transports

## Status

Completed.

## Accepted

- Added `WsTransport` with loopback-only endpoint validation.
- Added cdxm-managed WebSocket launch path using `codex app-server --listen ws://127.0.0.1:<port>`.
- Added newline-delimited `StdioTransport` using `codex app-server --listen stdio://`.
- Added Unix WebSocket-over-Unix-socket transport behind `cfg(unix)`.
- Kept all transports behind `AppServerTransport`.

## TDD Evidence

RED:

```text
test transport::ws::tests::rejects_non_loopback_ws ... FAILED
assertion failed: ensure_loopback_ws("ws://192.168.1.2:9").is_err()
```

GREEN:

```bash
cargo test --lib transport::ws::tests
```

Result:

```text
test result: ok. 1 passed; 0 failed
```

Compile:

```bash
cargo check
```

Result: passed.

Format:

```bash
cargo fmt --check
```

Result: passed.

## Remaining Risks

- P7 compiles transports but does not run a live Codex app-server smoke test; fake app-server coverage is deferred to P8.
